import { Coordinates } from "../../coordinates/entities/Coordinates";

class Company {
  id?: number;
  name: string;
  branch: string;
  cellphone: string;
  mail: string;
  plan: string;
}

export { Company };
